[features]
multi_agent = true

[agents]
max_threads = 20
max_depth = 2

[agents.minimax]
description = "Decompose complex feature requirements, convert UI designs into code, and autonomously create comprehensive refactoring plans."
config_file = "agents/minimax.toml"