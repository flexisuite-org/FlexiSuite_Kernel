[workspace]
resolver = "2"
members = [
    "kernel-core",
    "kernel-api",
    "tests/contract",
    "kernel-data",
    "kernel-data/migration",
    "kernel-runtime",
    "kernel-registry",
    "ops/linters",
    "kernel-archiver",
]

[workspace.dependencies]
ed25519-dalek = { version = "2.0", features = ["rand_core"] }
