[package]
name = "migration"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
sea-orm-migration = { version = "1.1.19", default-features = false, features = ["sqlx-postgres", "runtime-tokio-rustls"] }
